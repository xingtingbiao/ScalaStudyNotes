server:
  port: 7777
#  servlet:
#    context-path: /scala-boot
spring:
  datasource:
    # driver-class-name: com.mysql.jdbc.Driver
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: admin
    url: jdbc:mysql://localhost:3306/scalaboot?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf8&useSSL=false
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
